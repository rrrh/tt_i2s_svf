# Multiplierless State Variable Filter (SVF)

Do not expect this to work! This was a two week spare time speed run. 

A 16-bit digital audio filter designed for the Tiny Tapeout IHP SG13G2 process (1x2 tile). The core (maybe) implements an oversamplied filter that uses bit-shifting instead of hardware multipliers to control cutoff frequency and resonance.
